import {NextPage} from "next";
import Layout from "components/Layout";
import SectionHeader from "components/SectionHeader";
const TeaserPage: NextPage = () => {
    return (
        <Layout>
            <SectionHeader text="Teasers" />
        </Layout>
    );
}
export default TeaserPage;
